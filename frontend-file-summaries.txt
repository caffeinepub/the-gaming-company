{
  "frontend/src/index.css": "Global styles with a dark gaming theme using OKLCH color tokens, Orbitron/Rajdhani/Inter fonts, neon red accent, glow utilities, card hover effects, and nav active states.",
  "frontend/tailwind.config.js": "Extended Tailwind config with gaming font families (Orbitron as both `gaming` and `display`, Rajdhani, Inter), glow box-shadow tokens, pulse-glow animation, and a new `shake` keyframe/animation used by the AdminPage password gate error state.",
  "frontend/src/context/CartContext.tsx": "Shopping cart context with safe localStorage persistence using BigInt serialization/deserialization, try-catch guards around all storage access, and in-memory fallback if localStorage is unavailable.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend integration, now including `useOrders` (fetches all orders via `getOrders`) and `usePlaceOrder` (mutation that calls `placeOrder` with customer details and cart items, invalidates the `orders` cache on success), alongside the existing product and sponsor hooks.",
  "frontend/src/components/ProductCard.tsx": "Product card component with null-safety checks for all product fields, safe bigint-to-price conversion, image error handling, category badge, and Add to Cart button with error handling.",
  "frontend/src/components/SponsorCard.tsx": "Sponsor card component showing logo image, name, description, and \"Official Sponsor\" label with card hover animation.",
  "frontend/src/components/ProductForm.tsx": "Reusable product form with a 20+ entry smart auto-fill lookup table that detects popular consoles/games (Nintendo Switch, PS5, Xbox, Minecraft, etc.) by name and auto-populates image URL, description, category, and default price; shows a neon-accented \"Auto-filled\" badge and image preview when active, with all fields remaining editable.",
  "frontend/src/components/SponsorForm.tsx": "Reusable form for adding/editing sponsors with name, logo URL, and description fields.",
  "frontend/src/pages/StorePage.tsx": "Main store page with hero banner and category browsing; switched from `useProductsByCategory` to `useAllProducts` with client-side filtering using `BACKEND_TO_FRONTEND_CATEGORY` mapping, so products added via the admin panel appear in the correct category section without requiring a page reload.",
  "frontend/src/pages/CartPage.tsx": "Shopping cart page with item list, quantity controls, remove buttons, per-item subtotals, order summary panel, and checkout navigation; all with safe bigint price formatting.",
  "frontend/src/pages/CheckoutPage.tsx": "Multi-step checkout page that first shows an order summary then a customer details form (Full Name, Email, Phone, Street Address, City, Postcode) with inline validation; on submit calls the `usePlaceOrder` mutation with real backend integration, clears the cart on success, and shows a confirmation screen with the delivery address.",
  "frontend/src/pages/AdminPage.tsx": "Admin panel with PIN gate (6280) and three tabs — Products, Sponsors, and a new Orders tab; the Orders tab uses `useOrders` to fetch all placed orders and displays each as a card showing customer name, email, phone, delivery address, line items with quantities/prices, total, and timestamp, with loading/error/empty states.",
  "frontend/src/App.tsx": "Root app component with desktop sidebar navigation (Games, Controllers, Consoles, Accessories, Sponsors, Admin gear) and a new mobile bottom navigation bar that exposes all the same nav buttons including Admin; all buttons have active state highlighting and proper click handlers wired to view/category state.",
  "frontend/src/components/ErrorBoundary.tsx": "Reusable React class-based error boundary that catches runtime errors in the component tree, logs them, and displays a fallback UI with a reload button instead of a blank screen."
}